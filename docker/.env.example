# Docker Compose - Environment Variables Example
# Copie este arquivo para .env e configure os valores

# Service Authentication Token
# IMPORTANTE: Gere um token seguro usando os scripts em receiver-service/scripts/
# Este token deve ser o MESMO em ambos os serviços (marketplace e receiver)
SERVICE_SHARED_SECRET=dev-token-change-me-in-production
